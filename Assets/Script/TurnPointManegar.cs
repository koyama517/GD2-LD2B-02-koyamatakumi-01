using System.Collections;
using System.Collections.Generic;
using System.ComponentModel;
using UnityEngine;

public class TurnPointManegar : MonoBehaviour
{
    public bool isActive;

    //public GameObject;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {

       /*Ga ActivePoint turnPoint;
        GameObject activePoint = GameObject.Find("ActivePoint");
        turnPoint = activePoint.GetComponent<ActivePoint>();
        if (turnPoint != null)
        {
            if (turnPoint.isRide)
            {
                if (Input.GetKeyDown(KeyCode.Space))
                {
                    if (isActive)
                    {
                        isActive = false;
                    }
                    else
                    {
                        isActive = true;
                    }
                }
            }

            turnPoint.isActive = isActive;

        }

        PeaPoint peaPoint;
        GameObject peaPointObj = GameObject.Find("PeaPoint");
        peaPoint = peaPointObj.GetComponent<PeaPoint>();
        if (peaPoint != null) {
            if (peaPoint.isRide)
            {
                if (Input.GetKeyDown(KeyCode.Space))
                {
                    if (isActive)
                    {
                        isActive = false;
                    }
                    else
                    {
                        isActive = true;
                    }
                }
            }
            peaPoint.isActive = isActive;

        }*/



    }
}
